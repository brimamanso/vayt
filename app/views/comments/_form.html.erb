        <%= simple_form_for([@post, @post.comments.build]) do |f| %>
            <%= f.input :content, class: 'form-control',placeholder: "Write Your Comment Here...", label: "Comment below:"%>
            <%= f.button :submit, :class => 'btn-primary' %>
        <% end %>
